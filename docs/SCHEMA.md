# Live database schema (`app.*`)

> Auto-generated by `scripts/dump-schema.sh` from the live DB — do not edit by hand.
> Regenerate after every migration. Generated: 2026-07-14T06:45Z

## Tables

### `app.access_overrides`
```
email text NOT NULL
role text NOT NULL
created_at timestamp with time zone
```

### `app.allocations`
```
id uuid NOT NULL
employee_id uuid NOT NULL
project_id uuid NOT NULL
period_type USER-DEFINED NOT NULL
period_start date NOT NULL
allocation_percent numeric NOT NULL
notes text
created_by_employee_id uuid
overridden_by_employee_id uuid
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
```

### `app.app_config`
```
key text NOT NULL
value jsonb NOT NULL
updated_at timestamp with time zone
```

### `app.client_analytics`
```
id uuid NOT NULL
client_id uuid NOT NULL
report_label text NOT NULL
file_name text NOT NULL
file_path text NOT NULL
file_size_bytes bigint
metrics_data jsonb
posts_data jsonb
summary jsonb
uploaded_by uuid
uploaded_at timestamp with time zone
insights_cache jsonb
report_type text NOT NULL
demographics_data jsonb
visitor_metrics jsonb
data_through date
```

### `app.client_scope_discipline_needs`
```
id uuid NOT NULL
scope_item_id uuid NOT NULL
department_id uuid NOT NULL
percent_need numeric NOT NULL
specialty_note text
created_at timestamp with time zone NOT NULL
```

### `app.client_scope_items`
```
id uuid NOT NULL
client_id uuid NOT NULL
title text NOT NULL
scope_type text NOT NULL
description text
owner_employee_id uuid
sort_order integer NOT NULL
is_active boolean NOT NULL
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
start_month date
end_month date
```

### `app.client_standing_allocations`
```
id uuid NOT NULL
client_id uuid NOT NULL
employee_id uuid NOT NULL
department_id uuid NOT NULL
percent numeric NOT NULL
notes text
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
scope_item_id uuid
```

### `app.clients`
```
id uuid NOT NULL
name text NOT NULL
account_owner_employee_id uuid
is_active boolean NOT NULL
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
```

### `app.daily_tasks`
```
id uuid NOT NULL
employee_id uuid NOT NULL
task_date date
project_id uuid
task_title text NOT NULL
status USER-DEFINED NOT NULL
notes text
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
description text
deadline date
sort_order integer NOT NULL
recurring_task_id uuid
```

### `app.deal_stage_history`
```
id uuid NOT NULL
deal_id uuid NOT NULL
stage text NOT NULL
entered_at timestamp with time zone
exited_at timestamp with time zone
changed_by uuid
```

### `app.deals`
```
id uuid NOT NULL
deal_name text NOT NULL
stage text NOT NULL
poc_employee_id uuid
next_steps text
deadline date
notes text
engagement_type text
business_model text
termination_type text
amount numeric
client_id uuid
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
currency text NOT NULL
company text NOT NULL
```

### `app.department_utilization`
```
department text
month_start date
employee_count bigint
avg_month_allocation_percent numeric
latest_allocation_edit_at timestamp with time zone
```

### `app.departments`
```
id uuid NOT NULL
name text NOT NULL
approver_email USER-DEFINED
leave_tracking_enabled boolean NOT NULL
sort_order integer NOT NULL
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
```

### `app.employee_leave_cycles`
```
id uuid NOT NULL
employee_id uuid NOT NULL
cycle_start date NOT NULL
cycle_end date NOT NULL
pl_allocated numeric NOT NULL
cl_allocated numeric NOT NULL
sl_allocated numeric NOT NULL
archived_at timestamp with time zone
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
```

### `app.employee_utilization`
```
employee_id uuid
full_name text
department text
month_start date
month_allocation_percent numeric
last_allocation_edit_at timestamp with time zone
```

### `app.employees`
```
id uuid NOT NULL
auth_user_id uuid
email USER-DEFINED NOT NULL
full_name text NOT NULL
department_id uuid NOT NULL
employment_type USER-DEFINED NOT NULL
access_level USER-DEFINED NOT NULL
role_title text
capacity_percent numeric NOT NULL
leave_tracking_enabled boolean NOT NULL
approver_emails ARRAY NOT NULL
is_active boolean NOT NULL
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
direct_manager_email USER-DEFINED
date_of_birth date
current_city text
emergency_contact_name text
emergency_contact_phone text
personal_address text
photo_url text
onboarding_completed boolean NOT NULL
```

### `app.feature_request_replies`
```
id uuid NOT NULL
feature_request_id uuid NOT NULL
employee_id uuid NOT NULL
reply_text text NOT NULL
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
attachments jsonb
```

### `app.feature_request_upvotes`
```
feature_request_id uuid NOT NULL
employee_id uuid NOT NULL
created_at timestamp with time zone NOT NULL
```

### `app.feature_requests`
```
id uuid NOT NULL
employee_id uuid NOT NULL
request_text text NOT NULL
status USER-DEFINED NOT NULL
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
request_type USER-DEFINED NOT NULL
links jsonb NOT NULL
attachments jsonb
```

### `app.invoices`
```
id uuid NOT NULL
employee_id uuid NOT NULL
invoice_month text NOT NULL
file_name text NOT NULL
file_path text NOT NULL
file_size_bytes bigint
uploaded_at timestamp with time zone
notes text
invoice_type text NOT NULL
```

### `app.leave_cycle_archives`
```
id uuid NOT NULL
employee_id uuid NOT NULL
cycle_start date NOT NULL
cycle_end date NOT NULL
pl_allocated numeric NOT NULL
cl_allocated numeric NOT NULL
sl_allocated numeric NOT NULL
pl_taken numeric NOT NULL
cl_taken numeric NOT NULL
sl_taken numeric NOT NULL
pl_remaining numeric NOT NULL
cl_remaining numeric NOT NULL
sl_remaining numeric NOT NULL
archived_at timestamp with time zone NOT NULL
```

### `app.leave_cycle_policy`
```
id bigint NOT NULL
cycle_start_month integer NOT NULL
cycle_start_day integer NOT NULL
pl_allocation numeric NOT NULL
cl_allocation numeric NOT NULL
sl_allocation numeric NOT NULL
is_active boolean NOT NULL
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
```

### `app.leave_requests`
```
id uuid NOT NULL
employee_id uuid NOT NULL
leave_type USER-DEFINED NOT NULL
start_date date NOT NULL
end_date date NOT NULL
reason text
medical_certificate_url text
status USER-DEFINED NOT NULL
approver_emails ARRAY NOT NULL
decided_by_employee_id uuid
decided_at timestamp with time zone
decision_note text
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
is_half_day boolean NOT NULL
```

### `app.notification_log`
```
id uuid NOT NULL
kind USER-DEFINED NOT NULL
recipient_email USER-DEFINED NOT NULL
subject text NOT NULL
payload jsonb NOT NULL
status text NOT NULL
sent_at timestamp with time zone
created_at timestamp with time zone NOT NULL
```

### `app.onboarding_checklist_items`
```
id uuid NOT NULL
checklist_id uuid NOT NULL
phase USER-DEFINED NOT NULL
sort_order integer NOT NULL
title text NOT NULL
description text
is_auto boolean NOT NULL
auto_key text
is_completed boolean NOT NULL
completed_at timestamp with time zone
completed_by_employee_id uuid
created_at timestamp with time zone NOT NULL
```

### `app.onboarding_checklists`
```
id uuid NOT NULL
employee_id uuid NOT NULL
status USER-DEFINED NOT NULL
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
```

### `app.policy_acknowledgments`
```
id uuid NOT NULL
employee_id uuid NOT NULL
policy_key text NOT NULL
policy_version text
acknowledged_at timestamp with time zone NOT NULL
```

### `app.policy_document_versions`
```
id uuid NOT NULL
policy_document_id uuid NOT NULL
version text NOT NULL
content_html text NOT NULL
updated_by_employee_id uuid
created_at timestamp with time zone NOT NULL
```

### `app.policy_documents`
```
id uuid NOT NULL
policy_key text NOT NULL
title text NOT NULL
content_html text NOT NULL
version text NOT NULL
reminder_month integer
last_updated_by_employee_id uuid
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
```

### `app.projects`
```
id uuid NOT NULL
client_id uuid NOT NULL
name text NOT NULL
engagement_type USER-DEFINED NOT NULL
status text NOT NULL
owner_employee_id uuid
created_at timestamp with time zone NOT NULL
updated_at timestamp with time zone NOT NULL
```

### `app.public_holidays`
```
id uuid NOT NULL
holiday_date date NOT NULL
name text NOT NULL
created_at timestamp with time zone
```

### `app.recurring_tasks`
```
id uuid NOT NULL
employee_id uuid NOT NULL
task_title text NOT NULL
notes text
description text
day_of_month integer NOT NULL
is_active boolean NOT NULL
created_at timestamp with time zone NOT NULL
```

### `app.system_daily_cleanup_log`
```
cleanup_date date NOT NULL
ran_by_employee_id uuid
ran_at timestamp with time zone NOT NULL
```

## Enums

- `app.access_level`: employee, leadership, admin
- `app.allocation_period_type`: week, month
- `app.employment_type`: full-time, fractional
- `app.engagement_type`: retainer, project
- `app.feature_request_status`: requested, in_progress, done, archived
- `app.feature_request_type`: feature, bug
- `app.leave_status`: pending, approved, rejected, cancelled
- `app.leave_type`: PL, CL, SL
- `app.notification_kind`: daily_tasklist_reminder, weekly_allocation_reminder, leave_submitted, pending_leave_digest, invoice_upload_reminder, new_employee_joined, onboarding_welcome, policy_update_reminder, cron_heartbeat, analytics_upload_reminder, feature_reply, feature_request_reply, feature_request_upvote, feature_request_status, new_bug_report
- `app.onboarding_checklist_status`: active, completed
- `app.onboarding_phase`: pre_joining, day_one, week_one, month_one
- `app.task_status`: pending, in_progress, done, archived

## Functions (RPCs)

- `app.archive_leave_cycle_row(p_cycle_id uuid)` SECURITY DEFINER
- `app.create_daily_task(p_task_date date, p_task_title text, p_project_id uuid, p_status app.task_status, p_notes text, p_description text, p_deadline date)` SECURITY DEFINER
- `app.current_access_level()` SECURITY DEFINER
- `app.current_employee_id()` SECURITY DEFINER
- `app.current_request_email()` 
- `app.enforce_leadership_leave_superadmin()` SECURITY DEFINER
- `app.enforce_mapped_access_level()` SECURITY DEFINER
- `app.enforce_superadmin_role_changes()` SECURITY DEFINER
- `app.ensure_employee_profile()` SECURITY DEFINER
- `app.ensure_leave_cycle_before_request()` SECURITY DEFINER
- `app.ensure_leave_cycle_record(p_employee_id uuid, p_as_of_date date)` SECURITY DEFINER
- `app.get_leave_cycle_summary_for_employee(p_employee_id uuid, p_as_of_date date)` SECURITY DEFINER
- `app.get_leave_summaries_for_employees(p_employee_ids uuid[], p_as_of_date date)` SECURITY DEFINER
- `app.get_my_leave_cycle_summary(p_as_of_date date)` SECURITY DEFINER
- `app.home_whos_out(p_from date, p_until date)` SECURITY DEFINER
- `app.is_analytics_uploader()` SECURITY DEFINER
- `app.is_agency_email(input_email text)` 
- `app.is_deal_flow_viewer()` SECURITY DEFINER
- `app.is_invoice_viewer()` SECURITY DEFINER
- `app.is_leadership_or_admin()` SECURITY DEFINER
- `app.is_leave_tracking_enabled_for_employee(target_employee_id uuid)` SECURITY DEFINER
- `app.is_superadmin()` SECURITY DEFINER
- `app.leave_cycle_end(p_cycle_start date)` 
- `app.leave_cycle_start(p_date date)` 
- `app.mapped_access_level_for_email(input_email text)` 
- `app.normalize_employee_record()` SECURITY DEFINER
- `app.now_utc()` 
- `app.overlap_days(p_start_a date, p_end_a date, p_start_b date, p_end_b date)` 
- `app.prepare_leave_request()` SECURITY DEFINER
- `app.rollover_all_leave_cycles(p_as_of_date date)` SECURITY DEFINER
- `app.save_my_allocations(p_period_type app.allocation_period_type, p_period_start date, p_lines jsonb)` SECURITY DEFINER
- `app.set_allocation_updated_at()` 
- `app.set_deals_updated_at()` 
- `app.set_updated_at()` 
- `app.spawn_onboarding_checklist(p_employee_id uuid)` SECURITY DEFINER
- `app.submit_leave_request(p_leave_type app.leave_type, p_start_date date, p_end_date date, p_reason text, p_medical_certificate_url text, p_is_half_day boolean)` SECURITY DEFINER
- `app.superadmin_email()` 
- `app.touch_client_scope_items_updated_at()` 
- `app.touch_client_standing_allocations_updated_at()` 
- `app.trg_block_employee_self_escalation()` SECURITY DEFINER
- `app.update_my_profile(p_full_name text, p_department_name text, p_employment_type app.employment_type, p_capacity_percent numeric, p_date_of_birth date, p_current_city text, p_emergency_contact_name text, p_emergency_contact_phone text, p_personal_address text)` SECURITY DEFINER

## RLS policies

- `access_overrides` · **access_overrides_select_all** (SELECT): true
- `access_overrides` · **access_overrides_write_superadmin** (ALL): app.is_superadmin()
- `allocations` · **allocations_delete_own_or_leadership** (DELETE): ((employee_id = app.current_employee_id()) OR app.is_leadership_or_admin())
- `allocations` · **allocations_insert_own_or_leadership** (INSERT): 
- `allocations` · **allocations_select_all_authenticated** (SELECT): true
- `allocations` · **allocations_update_own_or_leadership** (UPDATE): ((employee_id = app.current_employee_id()) OR app.is_leadership_or_admin())
- `app_config` · **app_config_select_all** (SELECT): true
- `app_config` · **app_config_write_superadmin** (ALL): app.is_superadmin()
- `client_analytics` · **analytics_delete** (DELETE): app.is_analytics_uploader()
- `client_analytics` · **analytics_insert** (INSERT): 
- `client_analytics` · **analytics_select_all** (SELECT): true
- `client_analytics` · **analytics_update** (UPDATE): app.is_analytics_uploader()
- `client_scope_discipline_needs` · **scope_disc_needs_leadership_all** (ALL): app.is_leadership_or_admin()
- `client_scope_items` · **scope_items_leadership_all** (ALL): app.is_leadership_or_admin()
- `client_scope_items` · **scope_items_select_all** (SELECT): true
- `client_standing_allocations` · **standing_alloc_leadership_all** (ALL): app.is_leadership_or_admin()
- `clients` · **clients_delete_leadership_only** (DELETE): app.is_leadership_or_admin()
- `clients` · **clients_insert_own_or_leadership** (INSERT): 
- `clients` · **clients_select_all** (SELECT): true
- `clients` · **clients_update_own_or_leadership** (UPDATE): (app.is_leadership_or_admin() OR (account_owner_employee_id = app.current_employee_id()) OR app.is_deal_flow_viewer())
- `daily_tasks` · **daily_tasks_delete_own_or_leadership** (DELETE): ((employee_id = app.current_employee_id()) OR app.is_leadership_or_admin())
- `daily_tasks` · **daily_tasks_insert_own_or_leadership** (INSERT): 
- `daily_tasks` · **daily_tasks_select_all_authenticated** (SELECT): true
- `daily_tasks` · **daily_tasks_update_own_or_leadership** (UPDATE): ((employee_id = app.current_employee_id()) OR app.is_leadership_or_admin())
- `deal_stage_history` · **stage_history_insert** (INSERT): 
- `deal_stage_history` · **stage_history_select** (SELECT): true
- `deal_stage_history` · **stage_history_update** (UPDATE): (app.is_leadership_or_admin() OR app.is_deal_flow_viewer())
- `deals` · **deals_delete_dealflow** (DELETE): app.is_deal_flow_viewer()
- `deals` · **deals_delete_leadership** (DELETE): app.is_leadership_or_admin()
- `deals` · **deals_insert_dealflow** (INSERT): 
- `deals` · **deals_insert_leadership** (INSERT): 
- `deals` · **deals_select** (SELECT): true
- `deals` · **deals_update_dealflow** (UPDATE): app.is_deal_flow_viewer()
- `deals` · **deals_update_leadership** (UPDATE): app.is_leadership_or_admin()
- `deals` · **deals_update_poc** (UPDATE): (poc_employee_id = app.current_employee_id())
- `departments` · **departments_delete_leadership** (DELETE): app.is_leadership_or_admin()
- `departments` · **departments_insert_leadership** (INSERT): 
- `departments` · **departments_select_all** (SELECT): true
- `departments` · **departments_update_leadership** (UPDATE): app.is_leadership_or_admin()
- `employee_leave_cycles` · **employee_leave_cycles_delete_leadership** (DELETE): app.is_leadership_or_admin()
- `employee_leave_cycles` · **employee_leave_cycles_insert_leadership** (INSERT): 
- `employee_leave_cycles` · **employee_leave_cycles_select_self_or_leadership** (SELECT): ((employee_id = app.current_employee_id()) OR app.is_leadership_or_admin())
- `employee_leave_cycles` · **employee_leave_cycles_update_leadership** (UPDATE): app.is_leadership_or_admin()
- `employees` · **employees_delete_leadership** (DELETE): app.is_leadership_or_admin()
- `employees` · **employees_insert_leadership** (INSERT): 
- `employees` · **employees_select_all_authenticated** (SELECT): true
- `employees` · **employees_update_leadership** (UPDATE): app.is_leadership_or_admin()
- `employees` · **employees_update_own** (UPDATE): (auth_user_id = auth.uid())
- `feature_request_replies` · **feature_request_replies_delete_own_or_leadership** (DELETE): ((employee_id = app.current_employee_id()) OR app.is_leadership_or_admin())
- `feature_request_replies` · **feature_request_replies_insert_own** (INSERT): 
- `feature_request_replies` · **feature_request_replies_select_all** (SELECT): true
- `feature_request_replies` · **feature_request_replies_update_own** (UPDATE): (employee_id = app.current_employee_id())
- `feature_request_upvotes` · **feature_request_upvotes_delete_own** (DELETE): (employee_id = app.current_employee_id())
- `feature_request_upvotes` · **feature_request_upvotes_insert_own** (INSERT): 
- `feature_request_upvotes` · **feature_request_upvotes_select_all** (SELECT): true
- `feature_requests` · **feature_requests_delete_own_or_leadership** (DELETE): (app.is_leadership_or_admin() OR ((employee_id = app.current_employee_id()) AND (status = 'requested'::app.feature_request_status)))
- `feature_requests` · **feature_requests_insert_own** (INSERT): 
- `feature_requests` · **feature_requests_select_all** (SELECT): true
- `feature_requests` · **feature_requests_update_own_or_leadership** (UPDATE): ((employee_id = app.current_employee_id()) OR app.is_leadership_or_admin())
- `invoices` · **invoices_delete_own** (DELETE): (employee_id = app.current_employee_id())
- `invoices` · **invoices_insert_own** (INSERT): 
- `invoices` · **invoices_select_own** (SELECT): (employee_id = app.current_employee_id())
- `invoices` · **invoices_select_viewer** (SELECT): app.is_invoice_viewer()
- `leave_cycle_archives` · **leave_cycle_archives_delete_leadership** (DELETE): app.is_leadership_or_admin()
- `leave_cycle_archives` · **leave_cycle_archives_insert_leadership** (INSERT): 
- `leave_cycle_archives` · **leave_cycle_archives_select_self_or_leadership** (SELECT): ((employee_id = app.current_employee_id()) OR app.is_leadership_or_admin())
- `leave_cycle_archives` · **leave_cycle_archives_update_leadership** (UPDATE): app.is_leadership_or_admin()
- `leave_cycle_policy` · **leave_cycle_policy_delete_leadership** (DELETE): app.is_leadership_or_admin()
- `leave_cycle_policy` · **leave_cycle_policy_insert_leadership** (INSERT): 
- `leave_cycle_policy` · **leave_cycle_policy_select_all** (SELECT): true
- `leave_cycle_policy` · **leave_cycle_policy_update_leadership** (UPDATE): app.is_leadership_or_admin()
- `leave_requests` · **leave_requests_delete_leadership_only** (DELETE): app.is_leadership_or_admin()
- `leave_requests` · **leave_requests_insert_own** (INSERT): 
- `leave_requests` · **leave_requests_select_own_or_leadership** (SELECT): ((employee_id = app.current_employee_id()) OR app.is_leadership_or_admin())
- `leave_requests` · **leave_requests_update_own_pending_or_leadership** (UPDATE): ((employee_id = app.current_employee_id()) OR app.is_leadership_or_admin())
- `notification_log` · **notification_log_select_leadership** (SELECT): app.is_leadership_or_admin()
- `onboarding_checklist_items` · **employees_update_own_auto_items** (UPDATE): ((is_auto = true) AND (checklist_id IN ( SELECT onboarding_checklists.id    FROM app.onboarding_checklists   WHERE (onboarding_checklists.employee_id = app.current_employee_id()))))
- `onboarding_checklist_items` · **employees_view_own_items** (SELECT): (checklist_id IN ( SELECT onboarding_checklists.id    FROM app.onboarding_checklists   WHERE (onboarding_checklists.employee_id = app.current_employee_id())))
- `onboarding_checklist_items` · **leadership_delete_items** (DELETE): app.is_leadership_or_admin()
- `onboarding_checklist_items` · **leadership_insert_items** (INSERT): 
- `onboarding_checklist_items` · **leadership_update_items** (UPDATE): app.is_leadership_or_admin()
- `onboarding_checklist_items` · **leadership_view_all_items** (SELECT): app.is_leadership_or_admin()
- `onboarding_checklists` · **employees_view_own_checklist** (SELECT): (employee_id = app.current_employee_id())
- `onboarding_checklists` · **leadership_delete_checklists** (DELETE): app.is_leadership_or_admin()
- `onboarding_checklists` · **leadership_insert_checklists** (INSERT): 
- `onboarding_checklists` · **leadership_update_checklists** (UPDATE): app.is_leadership_or_admin()
- `onboarding_checklists` · **leadership_view_all_checklists** (SELECT): app.is_leadership_or_admin()
- `policy_acknowledgments` · **employees_create_own_acks** (INSERT): 
- `policy_acknowledgments` · **employees_view_own_acks** (SELECT): (employee_id = app.current_employee_id())
- `policy_acknowledgments` · **leadership_view_all_acks** (SELECT): app.is_leadership_or_admin()
- `policy_document_versions` · **admin_delete_policy_versions** (DELETE): (EXISTS ( SELECT 1    FROM app.employees e   WHERE ((e.auth_user_id = auth.uid()) AND e.is_active AND (e.access_level = 'admin'::app.access_level))))
- `policy_document_versions` · **admin_insert_policy_versions** (INSERT): 
- `policy_document_versions` · **admin_update_policy_versions** (UPDATE): (EXISTS ( SELECT 1    FROM app.employees e   WHERE ((e.auth_user_id = auth.uid()) AND e.is_active AND (e.access_level = 'admin'::app.access_level))))
- `policy_document_versions` · **all_view_policy_versions** (SELECT): true
- `policy_documents` · **admin_delete_policies** (DELETE): (EXISTS ( SELECT 1    FROM app.employees e   WHERE ((e.auth_user_id = auth.uid()) AND e.is_active AND (e.access_level = 'admin'::app.access_level))))
- `policy_documents` · **admin_insert_policies** (INSERT): 
- `policy_documents` · **admin_update_policies** (UPDATE): (EXISTS ( SELECT 1    FROM app.employees e   WHERE ((e.auth_user_id = auth.uid()) AND e.is_active AND (e.access_level = 'admin'::app.access_level))))
- `policy_documents` · **all_view_policies** (SELECT): true
- `projects` · **projects_delete_leadership_only** (DELETE): app.is_leadership_or_admin()
- `projects` · **projects_insert_own_or_leadership** (INSERT): 
- `projects` · **projects_select_all** (SELECT): true
- `projects` · **projects_update_own_or_leadership** (UPDATE): (app.is_leadership_or_admin() OR (owner_employee_id = app.current_employee_id()))
- `public_holidays` · **public_holidays_select_all** (SELECT): true
- `public_holidays` · **public_holidays_write_leadership** (ALL): app.is_leadership_or_admin()
- `recurring_tasks` · **recurring_tasks_own** (ALL): (employee_id = app.current_employee_id())
- `system_daily_cleanup_log` · **daily_cleanup_log_insert** (INSERT): 
- `system_daily_cleanup_log` · **daily_cleanup_log_select** (SELECT): true
